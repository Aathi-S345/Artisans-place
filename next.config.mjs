// In next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  // You can add other Next.js specific configurations here if needed,
  // but do NOT add a webpack configuration for CSS.
};

export default nextConfig;